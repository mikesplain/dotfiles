{ pkgs, ... }:
{
  # We don't need a separate neovim configuration since we use pwnvim
  # But we can provide fzf-lua as a system package for reference

  # Note: The fzf-lua plugin should be added to the pwnvim configuration
  # in the upstream repository. This is a temporary workaround.
}
